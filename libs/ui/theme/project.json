{
  "name": "ui-theme",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/ui/theme/src",
  "projectType": "library",
  "tags": ["scope:ui", "type:theme"],
  "// targets": "to see all targets run: nx show project ui-theme --web",
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "lintFilePatterns": ["libs/ui/theme/**/*.ts"]
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --project libs/ui/theme/tsconfig.json"
      }
    }
  }
}
