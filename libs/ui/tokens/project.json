{
  "name": "ui-tokens",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/ui/tokens/src",
  "projectType": "library",
  "tags": ["scope:ui", "type:tokens"],
  "// targets": "to see all targets run: nx show project ui-tokens --web",
  "targets": {
    "verify": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo \"No CSS verification configured yet for ui-tokens\""
      }
    }
  }
}
